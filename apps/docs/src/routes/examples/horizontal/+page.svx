---
title: Horizontal List
description: Horizontal sortable list using direction="horizontal" on DndDroppable.
---

# Horizontal List

Set `direction="horizontal"` on `DndDroppable` — previews are rendered automatically with the container's direction. The reorder logic is the same as the vertical example — only the layout direction changes.

<script lang="ts">
	import { ComponentPreview } from '$lib/components';
	import HorizontalExampleCode from "$lib/examples/HorizontalExample.svelte?raw";
	import HorizontalExample from "$lib/examples/HorizontalExample.svelte";
</script>

<ComponentPreview
	sources={[
		{ name: "HorizontalExample.svelte", code: HorizontalExampleCode, language: "svelte" }
	]}
>
    <HorizontalExample/>
</ComponentPreview>